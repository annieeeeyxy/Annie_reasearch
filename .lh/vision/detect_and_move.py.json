{
    "sourceFile": "vision/detect_and_move.py",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 3,
            "patches": [
                {
                    "date": 1770131477825,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1770131606190,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,15 @@\n import cv2\n import numpy as np\n \n # =========================\n-# Camera (USB = index 0)\n+# USB Camera (index 0)\n # =========================\n cap = cv2.VideoCapture(0, cv2.CAP_AVFOUNDATION)\n if not cap.isOpened():\n     print(\"❌ Camera not found\")\n     exit()\n-print(\"✅ Camera connected (index 0)\")\n+print(\"✅ Camera connected (color)\")\n \n # =========================\n # Printer parameters (rough)\n # =========================\n@@ -22,11 +22,15 @@\n     if not ret:\n         print(\"❌ Failed to read frame\")\n         break\n \n-    # --- simple detection pipeline ---\n+    # ---------- 1️⃣ 彩色原图（不动） ----------\n+    color_view = frame.copy()\n+\n+    # ---------- 2️⃣ 灰度用于检测 ----------\n     gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)\n     blur = cv2.GaussianBlur(gray, (7, 7), 0)\n+\n     _, thresh = cv2.threshold(\n         blur, 0, 255,\n         cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU\n     )\n@@ -36,33 +40,34 @@\n     )\n \n     if contours:\n         largest = max(contours, key=cv2.contourArea)\n+\n         if cv2.contourArea(largest) > 1000:\n             x, y, w, h = cv2.boundingRect(largest)\n             cx = x + w // 2\n             cy = y + h // 2\n \n-            # draw\n-            cv2.rectangle(frame, (x, y), (x+w, y+h), (0,255,0), 2)\n-            cv2.circle(frame, (cx, cy), 5, (0,0,255), -1)\n+            # ---------- 3️⃣ 在【彩色图】上画 ----------\n+            cv2.rectangle(color_view, (x, y), (x+w, y+h), (0,255,0), 2)\n+            cv2.circle(color_view, (cx, cy), 5, (0,0,255), -1)\n \n-            # pixel -> mm (very rough, will calibrate later)\n+            # ---------- 4️⃣ 像素 → G-code ----------\n             H, W, _ = frame.shape\n             target_x = cx / W * BED_X_MM\n             target_y = cy / H * BED_Y_MM\n \n             gcode = f\"G1 X{target_x:.1f} Y{target_y:.1f} Z{Z_SAFE} F3000\"\n \n-            print(\"➡️ Target detected\")\n+            print(\"➡️ Detected object\")\n             print(f\"   Pixel: ({cx}, {cy})\")\n             print(f\"   G-code: {gcode}\")\n \n-            # slow down spam\n             cv2.waitKey(300)\n \n-    cv2.imshow(\"Detect\", frame)\n-    cv2.imshow(\"Thresh\", thresh)\n+    # ---------- 5️⃣ 显示 ----------\n+    cv2.imshow(\"Color View + Detection\", color_view)\n+    cv2.imshow(\"Binary (for detection)\", thresh)\n \n     if cv2.waitKey(10) & 0xFF == ord('q'):\n         break\n \n"
                },
                {
                    "date": 1770131914660,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,15 @@\n import cv2\n import numpy as np\n \n # =========================\n-# USB Camera (index 0)\n+# Camera (USB = index 0)\n # =========================\n cap = cv2.VideoCapture(0, cv2.CAP_AVFOUNDATION)\n if not cap.isOpened():\n     print(\"❌ Camera not found\")\n     exit()\n-print(\"✅ Camera connected (color)\")\n+print(\"✅ Camera connected (index 0)\")\n \n # =========================\n # Printer parameters (rough)\n # =========================\n@@ -22,15 +22,11 @@\n     if not ret:\n         print(\"❌ Failed to read frame\")\n         break\n \n-    # ---------- 1️⃣ 彩色原图（不动） ----------\n-    color_view = frame.copy()\n-\n-    # ---------- 2️⃣ 灰度用于检测 ----------\n+    # --- simple detection pipeline ---\n     gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)\n     blur = cv2.GaussianBlur(gray, (7, 7), 0)\n-\n     _, thresh = cv2.threshold(\n         blur, 0, 255,\n         cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU\n     )\n@@ -40,34 +36,33 @@\n     )\n \n     if contours:\n         largest = max(contours, key=cv2.contourArea)\n-\n         if cv2.contourArea(largest) > 1000:\n             x, y, w, h = cv2.boundingRect(largest)\n             cx = x + w // 2\n             cy = y + h // 2\n \n-            # ---------- 3️⃣ 在【彩色图】上画 ----------\n-            cv2.rectangle(color_view, (x, y), (x+w, y+h), (0,255,0), 2)\n-            cv2.circle(color_view, (cx, cy), 5, (0,0,255), -1)\n+            # draw\n+            cv2.rectangle(frame, (x, y), (x+w, y+h), (0,255,0), 2)\n+            cv2.circle(frame, (cx, cy), 5, (0,0,255), -1)\n \n-            # ---------- 4️⃣ 像素 → G-code ----------\n+            # pixel -> mm (very rough, will calibrate later)\n             H, W, _ = frame.shape\n             target_x = cx / W * BED_X_MM\n             target_y = cy / H * BED_Y_MM\n \n             gcode = f\"G1 X{target_x:.1f} Y{target_y:.1f} Z{Z_SAFE} F3000\"\n \n-            print(\"➡️ Detected object\")\n+            print(\"➡️ Target detected\")\n             print(f\"   Pixel: ({cx}, {cy})\")\n             print(f\"   G-code: {gcode}\")\n \n+            # slow down spam\n             cv2.waitKey(300)\n \n-    # ---------- 5️⃣ 显示 ----------\n-    cv2.imshow(\"Color View + Detection\", color_view)\n-    cv2.imshow(\"Binary (for detection)\", thresh)\n+    cv2.imshow(\"Detect\", frame)\n+    cv2.imshow(\"Thresh\", thresh)\n \n     if cv2.waitKey(10) & 0xFF == ord('q'):\n         break\n \n"
                },
                {
                    "date": 1770131930971,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -60,9 +60,9 @@\n             # slow down spam\n             cv2.waitKey(300)\n \n     cv2.imshow(\"Detect\", frame)\n-    cv2.imshow(\"Thresh\", thresh)\n+    #cv2.imshow(\"Thresh\", thresh)\n \n     if cv2.waitKey(10) & 0xFF == ord('q'):\n         break\n \n"
                }
            ],
            "date": 1770131477825,
            "name": "Commit-0",
            "content": "import cv2\nimport numpy as np\n\n# =========================\n# Camera (USB = index 0)\n# =========================\ncap = cv2.VideoCapture(0, cv2.CAP_AVFOUNDATION)\nif not cap.isOpened():\n    print(\"❌ Camera not found\")\n    exit()\nprint(\"✅ Camera connected (index 0)\")\n\n# =========================\n# Printer parameters (rough)\n# =========================\nBED_X_MM = 220\nBED_Y_MM = 220\nZ_SAFE = 30\n\nwhile True:\n    ret, frame = cap.read()\n    if not ret:\n        print(\"❌ Failed to read frame\")\n        break\n\n    # --- simple detection pipeline ---\n    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)\n    blur = cv2.GaussianBlur(gray, (7, 7), 0)\n    _, thresh = cv2.threshold(\n        blur, 0, 255,\n        cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU\n    )\n\n    contours, _ = cv2.findContours(\n        thresh, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE\n    )\n\n    if contours:\n        largest = max(contours, key=cv2.contourArea)\n        if cv2.contourArea(largest) > 1000:\n            x, y, w, h = cv2.boundingRect(largest)\n            cx = x + w // 2\n            cy = y + h // 2\n\n            # draw\n            cv2.rectangle(frame, (x, y), (x+w, y+h), (0,255,0), 2)\n            cv2.circle(frame, (cx, cy), 5, (0,0,255), -1)\n\n            # pixel -> mm (very rough, will calibrate later)\n            H, W, _ = frame.shape\n            target_x = cx / W * BED_X_MM\n            target_y = cy / H * BED_Y_MM\n\n            gcode = f\"G1 X{target_x:.1f} Y{target_y:.1f} Z{Z_SAFE} F3000\"\n\n            print(\"➡️ Target detected\")\n            print(f\"   Pixel: ({cx}, {cy})\")\n            print(f\"   G-code: {gcode}\")\n\n            # slow down spam\n            cv2.waitKey(300)\n\n    cv2.imshow(\"Detect\", frame)\n    cv2.imshow(\"Thresh\", thresh)\n\n    if cv2.waitKey(10) & 0xFF == ord('q'):\n        break\n\ncap.release()\ncv2.destroyAllWindows()\n"
        }
    ]
}